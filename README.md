# EndpointFinder

#searches through a website and parses all the .js files and search through each of those files for endpoints (keywords and strings in keysList.txt) to find exposed api keys and sensitive information